{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile and Run with I/O",
      "type": "shell",
      "command": "cmd.exe",
      "args": [
        "/c",
        "echo Compiling ${file} && if \"%fileExt%\"==\".c\" ( gcc \"${file}\" -std=c11 -o \"${fileDirname}\\a.exe\" ) else ( g++ \"${file}\" -std=c++20 -o \"${fileDirname}\\a.exe\" ) && \"${fileDirname}\\a.exe\" < \"${fileDirname}\\input.txt\" > \"${fileDirname}\\output.txt\""
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "windows": {
        "env": {
          "fileExt": "${fileExtname}"
        }
      }
    }
  ]
}
